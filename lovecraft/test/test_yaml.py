import yaml

'''@todo
Move these into parse. Or move meta out of parse. Or decide it's fine.
In short, do something.
'''


def test_yaml():
    pass
    # with open('source/config.yaml', 'r') as infile:
    #     loaded = yaml.load(infile)
    #     # print yaml.dump(loaded)
    #     print(loaded['subtitle'])
    # assert True == False, 'Yaml needs work dawg'


if __name__ == '__main__':
    test_yaml()
